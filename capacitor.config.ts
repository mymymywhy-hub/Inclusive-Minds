import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.inclusiveminds.imstories',
  appName: 'IM Stories',
  webDir: 'dist'
};

export default config;
